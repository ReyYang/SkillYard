[project]
name = "skillyard"
version = "0.1.0"
description = "Mac-first local Skill Library Manager for AI agent skills"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
skillyard = "skillyard.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
