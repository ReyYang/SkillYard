# Fetch and preview update impact before updating

Source Tree updates will default to fetch plus Update Impact preview before any working tree is changed. A single repository update can affect multiple Library Entries and Exposures, so the product should show which skills and Hosts are affected before applying the update. Automatic pull is too opaque for a tool whose value is making local skill provenance and blast radius visible.
