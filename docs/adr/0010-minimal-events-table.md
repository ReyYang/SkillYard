# Minimal events table

We will include a minimal `events` table in the SQLite State File. Events record user-visible changes such as adding a Source Tree, creating or removing an Exposure, updating a Source Tree, and resolving Host Entry Conflicts. This gives users enough traceability to understand what changed on their machine without turning the product into a heavy audit or telemetry system.
