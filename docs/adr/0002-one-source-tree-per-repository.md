# One Source Tree per repository

We will store one Source Tree per upstream repository or managed source, not one Source Tree per skill. Each Library Entry points to a `SKILL.md` path inside that Source Tree, which preserves upstream repository structure, avoids duplicate clones, and lets update impact analysis show every exposed skill affected by one repository update.
