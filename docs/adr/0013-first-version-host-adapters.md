# First-version Host Adapters

The first version will include built-in Host Adapters for Codex, Claude Code, Cursor, and GitHub Copilot, with Codex as the default and best-supported path. These cover the common multi-agent audience without making the first release chase every host listed by the broader agent ecosystem. Additional Hosts can be added later through the same adapter shape.
