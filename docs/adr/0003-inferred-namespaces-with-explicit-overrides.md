# Inferred namespaces with explicit overrides

We will infer Namespace values from source metadata by default, while allowing explicit user overrides. This keeps ordinary GitHub skills predictable, but still supports product-level names such as `lark/lark-doc` when the desired management identity should not mirror the upstream owner or repository name exactly.
